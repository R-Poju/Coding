```c++
namespace Ui {     // 定义一个名为 Ui 的命名空间
    class ChatUserWid;  // 在命名空间内声明一个类（仅声明，未定义）
}                  // 命名空间结束
```



这样写的作用：

1. 减少编译依赖。当只需要使用类的指针/引用时，前向声明可避免包含庞大的头文件，加速编译
2. 解决循环依赖。如果A类用到B类，B类又用到A类，前向声明可打破循环引用
3. 隐藏实现细节。在头文件中只声明类，具体定义在 .cpp 文件中实现（类似Pimpl惯用法）



